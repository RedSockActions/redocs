name: docs-actions
run-name: UpdateDocs
on:
  workflow_dispatch:
    inputs:
      base:
        description: "Base of docs"
        default: ${{ github.event.repository.name }}
      redocs_config:
        description: "URL to redocs config"
        default: ""
jobs:
  tag-release:
    runs-on: ubuntu-latest
    steps:
      - uses: RedSockActions/redocs@v0.0.0